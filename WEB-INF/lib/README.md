Place runtime libraries here when deploying to a servlet container.

Original WAR included:

- mysql-connector-j-9.1.0.jar
- jstl.jar
- standard.jar

These JAR files are intentionally not committed. Prefer configuring them through your IDE, server runtime, or a build tool.
